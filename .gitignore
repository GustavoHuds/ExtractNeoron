# secrets & sessions — NEVER commit
.env
storageState.json
data/

# local reference catalog (business data — each deployment imports its own
# via the dashboard "Catálogo" button; never versioned)
/Catálogo - ref/

# node
node_modules/
npm-debug.log*

# os / editor
.DS_Store
Thumbs.db
.vscode/
